services:
  db:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_USER: csprep
      POSTGRES_PASSWORD: csprep
      POSTGRES_DB: csprep
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
