import { ComingSoon } from "@/components/shell/coming-soon";

export const metadata = { title: "Schedule" };

export default function SchedulePage() {
  return (
    <ComingSoon
      title="Schedule"
      phase="Phase 3"
      description="Daily study blocks with recurring habits and one-tap completion."
    />
  );
}
