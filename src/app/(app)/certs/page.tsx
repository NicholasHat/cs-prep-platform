import { ComingSoon } from "@/components/shell/coming-soon";

export const metadata = { title: "Certificates" };

export default function CertsPage() {
  return (
    <ComingSoon
      title="Certificates"
      phase="Phase 6"
      description="Track certifications in progress: provider, % complete, target dates."
    />
  );
}
