# Local Postgres — matches docker-compose.yml (run: docker compose up -d)
DATABASE_URL="postgresql://csprep:csprep@localhost:5432/csprep"

# Anthropic API key for the notes assistant — server-side only
ANTHROPIC_API_KEY=""
